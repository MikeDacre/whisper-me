# Whisper-ME Tasker Tools

Tasker project configurations to allow sending voice files over wireguard to a
whisper-me webserver and then receive text back that is copied to the clipboard
or written to a file.
