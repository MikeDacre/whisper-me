# Whisper.CPP Config

This directory contains configuration files and script designed to be able to
replicate a whisper build on a Mac OS machine in a way that will work well with
the `whisper-me` code.
