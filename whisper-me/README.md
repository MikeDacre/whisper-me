# Whisper-ME Python Web Server

This project runs a python webserver on a computer with whisper-cpp available.

It exposes a webhook on a port defined by `PORT` in the config file, default is
`443`. IP address must be specified via `IP`. This is intended to be used with
wireguard.

